import { FileTypeToClassCssPipe } from './file-type-to-class-css.pipe';

describe('FileTypeToClassCssPipe', () => {
  it('create an instance', () => {
    const pipe = new FileTypeToClassCssPipe();
    expect(pipe).toBeTruthy();
  });
});
